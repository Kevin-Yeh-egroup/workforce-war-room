---
status: implementation-note
project: workforce-war-room
audience: Kevin 與馴錢師星展專案工作管理者
updated: 2026-08-06
---

# Workforce War Room 設計決策紀錄

> 本文件不是硬性 UI 規格。介面可依實際 InfoCenter 資料、管理決策效率與行動版可讀性調整；目前實作以 2026-08-06 的唯讀工作摘要版本為準。

## 產品目的

這不是人資名冊，也不是另一套派工資料庫。它是一個管理決策入口，讓管理者在一個畫面先回答：

1. 現在有哪些工作需要處理或派工？
2. 工讀生目前的狀態與可分配線索為何？
3. 完整工作流程中，哪些階段已連接資料、哪些仍有缺口？
4. 今天最值得採取的下一步是什麼？

InfoCenter 保持為工作、事件、驗收與薪資的正式紀錄；本頁只做唯讀彙整、例外提示與工作建議。

## 核心原則

- 例外優先：先顯示逾期、待派、資料過期與待確認事項。
- 意圖優先：設定審核中心先讓管理者選擇「欄位、範本、計算、執行證據」，同一時間只展開一個決策工作區。
- 分層揭露：第一層看狀態與主要動作，第二層進行比較與模擬，第三層才展開公式、欄位與檢查表細節。
- 來源透明：每一個數字都顯示資料日期；過期資料不得偽裝成即時狀態。
- 管理語言：使用「待派工作、待驗收、可投入、需覆核」等台灣職場常用詞。
- 隱私最小化：主頁只顯示遮罩姓名與遮罩 Email，不顯示電話、完整 Email、地址、銀行、薪資金額、原始事件標題或個人長篇備註。
- 正式操作回到 InfoCenter：本頁不直接派薪、不直接結案、不在未核准下寫入工作。

## 資訊架構

- 總覽：今日重點、資料警示、核心指標、工作流程與優先建議。
- 工作盤面：由 InfoCenter 唯讀摘要彙整，顯示匿名工作類型、狀態、期限、能力門檻、P50 / P80、樣本信心與人員涵蓋。
- 工讀生狀態：只顯示 active 成員、近期工時快照、派薪狀態與工作訊號；paused 成員不進入本頁或派工建議，姓名與 Email 一律遮罩。
- 工作藍圖：候選、待派、執行、驗收、完成、結算六個階段，以及資料是否已接通。
- 工作建議：依資料過期、逾期任務、工作量與缺少欄位產生管理建議。

## 視覺系統

- 背景：暖灰白，降低長時間管理介面的疲勞。
- 主色：深墨綠，代表穩定與確認；警示使用琥珀與磚紅，不使用高飽和霓虹色。
- 字體：系統無襯線字體，優先 Noto Sans TC / Microsoft JhengHei。
- 行動版本文至少 16px，行高 1.65；不用手動換行切碎中文句子。
- 卡片只用於獨立資訊單位，不做多層卡片巢狀。
- 圖像策略：不需要照片或生成圖片；工作流程、資料新鮮度與負荷使用程式化狀態條與節點呈現。
- 動效策略：只用於視圖切換、狀態回饋與數值載入；支援 `prefers-reduced-motion`，不使用無功能的連續視差或裝飾動畫。

## 互動與狀態

- 分頁使用真正按鈕與可見焦點。
- 待派工作可依全部、逾期、可交辦、需決策篩選。
- 工讀生狀態可搜尋遮罩姓名與依 active / paused 篩選。
- 「重新讀取」只重新抓取靜態資料，不寫入外部系統。
- InfoCenter 操作按鈕一律開啟正式系統；正式建立、派工、驗收與薪資仍需使用者核准。
- 設定審核中心提供證據模擬器，讓管理者直接測試「指派、活動、交付、送審、卡關、人工核准」如何形成狀態；AI 只在交付物可讀且已送審時提供預檢。
- 能力資訊使用社工、財務、AI、馴錢師四個獨立面向；每人固定顯示四項「知能證據指數」，不把時數直接包裝成績效或正式能力評等。
- 工作詳情可顯示知能適配候選與權重，但必須同時揭露本週容量是否已確認，且不得自動派工。
- 每張人員卡顯示總 XP、主能力證據、量化薪資階段候選與下一階段差距；量化候選不得使用「已升級」或「可調薪」等定論語氣。
- 薪資區只說明審查資格與人工核准邊界，不呈現金額或自動升級承諾。

## 響應式規則

- 桌面：左側導覽、右側主內容，總覽採 12 欄配置。
- 900px 以下：導覽移到頂部，主要區塊改為單欄或雙欄。
- 430px 以下：指標兩欄、列表單欄、按鈕至少 44px，禁止水平溢位。

## 驗收條件

- 首頁不再讀取 `data/dashboard.json`，並優先使用 `data/infocenter-work-summary.json`。
- 公開頁只讀取 `talent-pool.public.json`；`talent-pool.internal.json` 不得部署或由公開頁載入。
- 資料讀取失敗時有清楚的空狀態，不顯示 `undefined`。
- 可從總覽切換到四個主要管理面向。
- 390px 與桌面尺寸無水平溢位，主要按鈕可操作。
- 正式部署前完成桌面與 390px 行動版的人類操作驗證。
